/**
 * Liga/desliga integrações inteiras (cron, rotas de sync, dashboard).
 * Desativação é reversível: virar o valor para true reativa a integração
 * sem precisar reverter nenhuma outra mudança.
 */
module.exports = {
  SMARTLEADER_ENABLED: false
};
